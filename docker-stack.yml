services:
  api:
    image: ghcr.io/aloiscrr/notes-ai-agent-api:main
    labels:
      - "traefik.enable=true"
      - "traefik.http.services.web.loadbalancer.server.port=8000"
      - "traefik.http.routers.web.rule=Host(`notes-ai-agent-api.aloiscrr.dev`)"
      - "traefik.http.routers.web.entrypoints=websecure"
      - "traefik.http.routers.web.tls.certresolver=myresolver"
    secrets:
      - notes_ai_agent_database_url
      - notes_ai_agent_gemini_api_key
      - notes_ai_agent_supabase_jwt_secret
      - notes_ai_agent_openai_api_key
    deploy:
      update_config:
        order: start-first
    networks:
      - traefik-public
    

secrets:
  notes_ai_agent_database_url:
    external: true
  notes_ai_agent_gemini_api_key:
    external: true
  notes_ai_agent_supabase_jwt_secret:
    external: true
  notes_ai_agent_openai_api_key:
    external: true

networks:
  traefik-public:
    external: true